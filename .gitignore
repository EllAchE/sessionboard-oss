node_modules/
.next/
.open-next/
.wrangler/
next-env.d.ts
*.tsbuildinfo
.env
.env.*
.envrc
!.env.example
.DS_Store
coverage/
.cicero/
# Agent tooling scratch space. Claude Code puts its built-in worktrees under
# `.claude/worktrees/`, which are full checkouts of this repo — tracking them would commit a
# second copy of the tree. Lint and vitest also ignore this directory; see `eslint.config.mjs`
# and `vitest.config.ts`.
.claude/

# Per-workstream scratch notes from the build sessions.
/tasks/
.vercel
